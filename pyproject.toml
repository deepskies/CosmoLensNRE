[tool.poetry]
name = "deeptemplate-science"
version = "0.1.0"
description = "a template for a science motivated project, focus on ease of reproducibility"
authors = ["Jane Doe <jane@uni.edu>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.group.dev.dependencies]
pre-commit = "^3.3.2"
pytest-cov = "^4.1.0"

[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"
